name: docs

on:
  push:
  workflow_dispatch:

jobs:
  docs:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install pandoc
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc

      - name: Install typst (official binary)
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y curl tar xz-utils

          TYPST_VER="0.13.0"   # you can bump this later
          URL="https://github.com/typst/typst/releases/download/v${TYPST_VER}/typst-x86_64-unknown-linux-gnu.tar.xz"

          echo "Downloading: $URL"
          curl -fL --retry 5 --retry-delay 2 -o typst.tar.xz "$URL"

          # sanity check (should be megabytes, not 9 bytes)
          ls -lh typst.tar.xz
          file typst.tar.xz

          mkdir -p typst_tmp
          tar -xJf typst.tar.xz -C typst_tmp

          sudo install -m 0755 "$(find typst_tmp -type f -name typst | head -n 1)" /usr/local/bin/typst
          typst --version

      - name: Setup python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install python deps
        run: |
          pip install -r requirements.txt || true

      - name: Check docs
        run: ./tt/tt_tool.py --check-docs

      - name: Create PDF
        run: ./tt/tt_tool.py --create-pdf

      - name: Upload datasheet
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: datasheet
          path: datasheet.pdf